import { StyleSheet } from 'react-native'

/**
 * Styles for DashBoard folder customed components
 * contains all styles applied for the component
 * @type {Object}
 */
const styles = StyleSheet.create({
  container: {
    justifyContent: 'center',
    alignItems: 'center',
    padding: '5%'
  }
})

export default styles
