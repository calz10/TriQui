import { StyleSheet } from 'react-native'

/**
 * Styles for Quiz folder customed components
 * contains all styles applied for the component
 * @type {Object}
 */
const styles = StyleSheet.create({
  quizContentView: {
    height: '80%',
    alignContent: 'center',
    alignItems: 'center'
  },
  quizContent: {
    height: '100%',
    margin: '5%'
  },
  resultContent: {
    height: '100%',
    flex: 2.5
  }
})

export default styles
